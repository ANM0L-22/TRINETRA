# Trinetra — All Dependencies
# Install: pip install -r requirements.txt

# ── Core AI / CV ──────────────────────────────────────────────
ultralytics>=8.2.0
opencv-python-headless>=4.9.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# ── OCR (for real number plate recognition) ──────────────────
# PaddleOCR currently requires Python versions with published PaddlePaddle wheels.
# Keep these optional so Streamlit deploys on unsupported interpreters, while
# still enabling OCR on Python 3.11/3.12 where wheels are available.
paddlepaddle>=2.6.1,<3.0.0; python_version < "3.13"
paddleocr>=2.7.3; python_version < "3.13"

# ── Tracking ─────────────────────────────────────────────────
deep-sort-realtime>=1.3.2

# ── Dashboard ────────────────────────────────────────────────
streamlit>=1.28.0
plotly>=5.18.0
pydeck>=0.8.0
pandas>=2.0.0

# ── Data / utils ─────────────────────────────────────────────
PyYAML>=6.0
Pillow>=10.0.0
tqdm>=4.65.0
scipy>=1.11.0
loguru>=0.7.0
python-dotenv>=1.0.0
requests>=2.31.0
roboflow>=1.1.0
